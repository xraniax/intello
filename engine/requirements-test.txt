# Test dependencies for the engine service
# Install alongside main requirements:  pip install -r requirements.txt -r requirements-test.txt

pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0
pytest-cov>=4.1.0
httpx>=0.26.0          # ASGI test client for FastAPI (no running server needed)
